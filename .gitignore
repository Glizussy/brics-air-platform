# Secrets — NEVER commit real keys
.env
*.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.envrc

# Prefetched demo data — TRACKED on purpose (Step 4: cold-start fallback).
# Do NOT ignore backend/cache/*.json; demo must work with zero API keys.

# SQLite local DB
*.db
*.sqlite3
*.db-wal
*.db-shm

# OS / editors
.DS_Store
.vscode/
.idea/
